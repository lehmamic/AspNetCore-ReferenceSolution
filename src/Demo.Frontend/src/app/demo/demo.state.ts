import { RouterReducerState } from '@ngrx/router-store';

// tslint:disable-next-line:no-empty-interface
export interface DemoState {
}

export const initialDemoState: DemoState = {
};

export const DEMO_STATE_FEATURE_KEY = 'demo';
